# Supabase Postgres — use BOTH urls for Prisma.
# Pooled (pgbouncer) for runtime queries, direct for migrations/import.
#
# Supabase dashboard → Project → Connect → ORMs → Prisma → copy values.
# The URL-encoded password matters; wrap the value in quotes if it has specials.

# Pooled (transaction mode) — runtime.
DATABASE_URL="postgresql://postgres.yxfipkfkpltodciywnwz:[PASSWORD]@aws-0-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"

# Direct — migrations & bulk import.
DIRECT_URL="postgresql://postgres:[PASSWORD]@db.yxfipkfkpltodciywnwz.supabase.co:5432/postgres"

# Path to the dataset CSV used by scripts/import-data.ts
DATASET_CSV="_dataset_raw/EAFC26.csv"
